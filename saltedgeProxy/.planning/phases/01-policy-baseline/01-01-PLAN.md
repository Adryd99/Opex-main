---
phase: 01-policy-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/application.properties
  - src/main/java/com/saltedgeproxy/app/saltedgeproxy/SaltedgeproxyApplication.java
  - src/main/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyProperties.java
  - src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionService.java
  - src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/InvalidPolicyVersionException.java
  - src/test/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyPropertiesTest.java
  - src/test/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionServiceTest.java
autonomous: true
requirements: [POLI-01, POLI-02]
user_setup: []
must_haves:
  truths:
    - "The application reads one configured current privacy-policy version from a single authoritative source."
    - "Future consent-capture code can ask a shared service for the current policy version instead of hardcoding or trusting request-side assumptions."
    - "Submitted policy versions that match the configured current version are accepted by the shared validation logic, and mismatched versions are rejected before persistence logic would run."
  artifacts:
    - path: "src/main/resources/application.properties"
      provides: "Current privacy-policy version configuration key"
      contains: "gdpr.policy.current-version"
    - path: "src/main/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyProperties.java"
      provides: "Typed configuration binding for GDPR policy settings"
      min_lines: 15
    - path: "src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionService.java"
      provides: "Shared current-version lookup and validation logic"
      min_lines: 20
    - path: "src/test/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionServiceTest.java"
      provides: "Proof that matching versions pass and mismatches fail"
      min_lines: 20
  key_links:
    - from: "src/main/java/com/saltedgeproxy/app/saltedgeproxy/SaltedgeproxyApplication.java"
      to: "src/main/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyProperties.java"
      via: "Spring configuration-properties scanning"
      pattern: "ConfigurationPropertiesScan|EnableConfigurationProperties"
    - from: "src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionService.java"
      to: "src/main/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyProperties.java"
      via: "Constructor injection of typed configuration"
      pattern: "GdprPolicyProperties"
---

<objective>
Establish the single authoritative privacy-policy version source for the application and provide reusable validation logic for later consent capture work.

Purpose: Phase 1 removes future request-side assumptions by making policy version checks come from one typed Spring configuration path and one shared validation service.
Output: Bound GDPR policy configuration, a reusable version-validation service with a clear failure contract, and focused tests proving match/mismatch behavior.
</objective>

<execution_context>
@C:/Users/adria/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/adria/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/resources/application.properties
@src/main/java/com/saltedgeproxy/app/saltedgeproxy/SaltedgeproxyApplication.java
@src/main/java/com/saltedgeproxy/app/saltedgeproxy/controller/UserController.java
@src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/SaltEdgeService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind the current GDPR policy version through typed application configuration</name>
  <files>src/main/resources/application.properties, src/main/java/com/saltedgeproxy/app/saltedgeproxy/SaltedgeproxyApplication.java, src/main/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyProperties.java</files>
  <action>Add a dedicated `gdpr.policy.current-version` property to application configuration and expose it through a typed Spring configuration-properties class. Enable configuration-properties scanning from the application bootstrap instead of introducing ad hoc `@Value` lookups for policy version checks. Keep the scope narrow: this task establishes the authoritative configuration source only and must not start the consent endpoint itself.</action>
  <verify>.\mvnw.cmd -q -DskipTests compile</verify>
  <done>The application has a single typed source for the current privacy-policy version, and future code can inject it without duplicating property names.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable policy-version validation logic for future consent capture</name>
  <files>src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionService.java, src/main/java/com/saltedgeproxy/app/saltedgeproxy/service/InvalidPolicyVersionException.java</files>
  <action>Implement a shared service responsible for exposing the configured current policy version and validating submitted versions against it. Give mismatches a clear failure path via a dedicated exception or equivalent explicit contract so later consent-recording code can reject stale or unknown versions before any persistence happens. Do not wire new API endpoints in this phase; keep the logic reusable and framework-native for later controller integration.</action>
  <verify>.\mvnw.cmd -q -DskipTests compile</verify>
  <done>A single service can answer "what is the current policy version?" and reject mismatched submitted versions before any storage logic would run.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for properties binding and version matching behavior</name>
  <files>src/test/java/com/saltedgeproxy/app/saltedgeproxy/config/GdprPolicyPropertiesTest.java, src/test/java/com/saltedgeproxy/app/saltedgeproxy/service/GdprPolicyVersionServiceTest.java</files>
  <action>Add focused tests that prove the bound GDPR policy version is loaded correctly and that the validation service accepts matching versions while rejecting mismatches. Prefer fast, narrow tests over controller or full-stack coverage in this phase, because the goal here is the policy baseline rather than the consent endpoint. If a small Spring slice is needed for property binding, keep it minimal and isolated.</action>
  <verify>.\mvnw.cmd test -Dtest=GdprPolicyPropertiesTest,GdprPolicyVersionServiceTest</verify>
  <done>Automated tests demonstrate that the authoritative version source is real and that version validation behavior is correct for both accepted and rejected inputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.\mvnw.cmd test -Dtest=GdprPolicyPropertiesTest,GdprPolicyVersionServiceTest` passes
- [ ] No raw GDPR policy-version literals are introduced outside `application.properties` and the typed configuration class
- [ ] The validation service rejects mismatched policy versions before any future consent persistence path would run
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The application exposes one authoritative current privacy-policy version via typed Spring configuration
- Shared validation logic exists and is ready for later consent-capture integration
- Focused tests prove both matching and mismatched version behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-policy-baseline/01-01-SUMMARY.md`
</output>
